fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
python-dotenv>=1.0.0

# ClearCut pipeline (use from project root: pip install -e . && pip install -r backend/requirements.txt)
# Parent package provides: app (pipeline)
